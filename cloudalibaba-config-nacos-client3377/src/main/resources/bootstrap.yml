server:
  port: 3377

spring:
  application:
    name: nacos-config-client

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml #指定yaml格式的配置
#        group: DEV_GROUP
        namespace: cc46edf7-398a-482c-9fa2-5aef190e15e6

# DataId -> nacos-config-client-dev.yaml